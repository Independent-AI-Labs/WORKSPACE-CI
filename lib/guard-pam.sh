# lib/guard-pam.sh - prior pam helpers (scrub only; git install uses host-exec)
#
# pam_cap git delivery was removed. Scrub helpers live in guard-host-exec.sh.
# This file remains so existing source lines do not break mid-migration.