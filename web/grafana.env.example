# Dev: direct to Gateway Grafana on VM loopback (browser must run on the same host).
GRAFANA_BASE_URL=http://127.0.0.1:3030
# Home landing at / (catalogue at /projects). Omit or set false to show catalogue on /.
WIKI_HOME_LANDING_ENABLED=true