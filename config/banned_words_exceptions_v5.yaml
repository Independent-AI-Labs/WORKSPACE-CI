# Temporarily emptied: the deployed v5 engine loads both the sealed and
# repo copies of this file and rejects identical entries as duplicates,
# so the repo copy must stay empty for one commit generation. It is
# restored verbatim by the next commit, once the deploy carrying the
# single-load fix (repo config wins over the sealed default) is sealed.
exceptions: []
